import { createTheme } from '@mui/material/styles'

export const theme = createTheme({
  palette: {
    primary: {
      main: '#ff9900',
      contrastText: '#000'
    },
    secondary: {
      main: '#000',
      contrastText: '#fff'
    }
  }
})
